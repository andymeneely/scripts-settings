set shiftwidth=4  "How much to indent by on automatic reindents
set expandtab     "Hitting Tab results in spaces, not a tab
set tabstop=4     "Assume 4 spaces counts as an indent
set softtabstop=4 "Treat multiple spaces like a tab so Backspace is not repetitive
set autoindent    "Hitting return will automatically indent

