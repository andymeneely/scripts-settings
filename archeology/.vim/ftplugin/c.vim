set tabstop=4		"Number of spaces to display a tab as
set softtabstop=4	"Number of spaces to get to the next tab (when it equals tabstop and with noexpandtab it means ALWAYS use tabs)
set noexpandtab		"Don't expand tabs into spaces when I hit tab
set shiftwidth=4	"Number of columns text is indented with reindent operations
set cindent
