#/bin/bash

$ZIP=$1

# mkdir

# unzip

# Make this with digits
# find . -name "*palindrome\(1\).c" -exec rm -rf {} \;

# ls *palindrome.c | awk '{split($0,a," - "); system("mkdir \""a[1]"\"") }'

#  ls *palindrome.c | awk '{split($0,a," - "); system("cp \""$0"\" \""a[1]"\"/palindrome.c") }'
